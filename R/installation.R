# Modify this path to the directory where your zip files are located
package_dir <- "P:/10619/Rpackages"

# Install packages from zip files
install.packages(file.path(package_dir, "backports_1.4.1.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "base64enc.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "broom.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "bslib.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "cachem.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "cli.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "clipr.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "commonmark.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "crayon.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "crul.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "curl.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "cchsflow.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "data.table.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "DBI.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "digest.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "dplyr.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "ellipsis.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "evaluate.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "fansi.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "fastmap.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "flextable.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "fontawesome.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "fontBitstreamVera.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "fontquiver.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "forcats.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "gfonts.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "generics.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "gdtools.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "glue.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "graphics.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "grDevices.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "grid.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "gtable.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "haven.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "highr.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "hms.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "htmltools.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "httpuv.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "insight.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "jsonlite.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "knitr.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "lattice.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "lifecycle.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "logger.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "Matrix.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "magrittr.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "methods.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "mice.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "mime.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "minqa.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "mitools.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "numDeriv.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "officer.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "pastecs.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "pillar.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "pkgconfig.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "purrr.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "R6.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "ragg.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "Rcpp.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "RcppArmadillo.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "readr.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "recodeflow.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "rlang.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "rmarkdown.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "shiny.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "sjlabelled.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "sourcetools.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "splines.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "stats.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "stringi.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "stringr.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "survey.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "survival.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "systemfonts.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "tibble.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "tidyselect.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "tidyr.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "tinytex.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "tzdb.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "utf8.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "uuid.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "vctrs.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "vroom.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "webshot.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "withr.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "xfun.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "xml2.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "xtable.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "yaml.zip"), repos = NULL, type = "win.binary")
install.packages(file.path(package_dir, "zip.zip"), repos = NULL, type = "win.binary")